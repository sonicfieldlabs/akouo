{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://akouo.dev/schemas/claim-taxonomy.schema.json",
  "title": "akoúō Claim Taxonomy",
  "description": "Shared claim taxonomy for separating heard, measured, inferred, interpreted, speculative, and undetermined claims.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "heard",
    "measured",
    "inferred",
    "interpreted",
    "speculative",
    "undetermined"
  ],
  "properties": {
    "heard": {
      "$ref": "#/$defs/claim_list",
      "description": "Claims directly present in the audio, prompt, transcript, field note, or supplied description."
    },
    "measured": {
      "$ref": "#/$defs/claim_list",
      "description": "Claims produced by file, signal, waveform, spectrogram, metadata, or other technical inspection."
    },
    "inferred": {
      "$ref": "#/$defs/claim_list",
      "description": "Plausible logical, technical, or forensic deductions that are not directly confirmed by available evidence. Do not place cultural theory, affective readings, or symbolic interpretation here."
    },
    "interpreted": {
      "$ref": "#/$defs/claim_list",
      "description": "Cultural, theoretical, affective, ecological, archival, political, or contextual readings."
    },
    "speculative": {
      "$ref": "#/$defs/claim_list",
      "description": "Declared fictional, symbolic, imaginative, mythic, or possible-world readings."
    },
    "undetermined": {
      "$ref": "#/$defs/claim_list",
      "description": "Claims that cannot be responsibly made from the available input."
    }
  },
  "$defs": {
    "confidence": {
      "type": "string",
      "enum": ["high", "medium", "low", "undetermined"]
    },
    "claim": {
      "type": "object",
      "additionalProperties": false,
      "required": ["statement", "confidence"],
      "properties": {
        "statement": {
          "type": "string",
          "minLength": 1
        },
        "confidence": {
          "$ref": "#/$defs/confidence"
        },
        "basis": {
          "type": "string",
          "description": "Short note describing what supports or limits the claim."
        },
        "source": {
          "$ref": "#/$defs/claim_source",
          "description": "Optional v0.6 field naming which apparatus or evidence stream produced this claim, so DSP facts, model perception, transcripts, memory comparisons, and human notes never blur."
        },
        "time_range": {
          "$ref": "#/$defs/time_range",
          "description": "Optional v0.6 field anchoring the claim to a time window in the supplied audio. Use it to resist invented temporal order: a claim without a time anchor is a claim about the whole object."
        }
      }
    },
    "claim_source": {
      "type": "string",
      "enum": [
        "audio",
        "dsp",
        "metadata",
        "model",
        "transcript",
        "context",
        "memory",
        "human",
        "other"
      ]
    },
    "time_range": {
      "type": "object",
      "additionalProperties": false,
      "required": ["start_s", "end_s"],
      "properties": {
        "start_s": { "type": "number", "minimum": 0 },
        "end_s": { "type": "number", "minimum": 0 }
      }
    },
    "claim_list": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/claim"
      },
      "default": []
    }
  }
}
